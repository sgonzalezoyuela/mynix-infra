# JOSSO Proxy

TODO: Make a flake template to create proxies providing certs, etc.

nginx server using self-singed certificates.  The conainer will bind the network to the host to be able to access JOSSO.

# Use Makefile

Check run target for image config.
